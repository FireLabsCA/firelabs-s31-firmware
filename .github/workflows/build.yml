name: build

on:
  push:
    branches: [main]
  pull_request:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  firmware:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Cache PlatformIO
        uses: actions/cache@v4
        with:
          path: |
            ~/.platformio
            ~/.cache/pip
          key: pio-${{ runner.os }}-${{ hashFiles('platformio.ini') }}

      - name: Install PlatformIO
        run: pip install platformio

      - name: Build (s31)
        run: pio run -e s31

      - name: Stage binary
        run: |
          mkdir -p dist
          # GITHUB_REF_NAME is "<pr>/merge" on pull_request; the slash would be
          # read as a directory. Flatten it so the cp target is a plain filename.
          ref="${GITHUB_REF_NAME//\//-}"
          cp .pio/build/s31/firmware.bin "dist/firelabs-s31-${ref}.bin"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: firmware
          path: dist/*.bin

      - name: Attach to release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*.bin
