#pragma once

// Brand/identity strings. Kept here, not in build_flags, because the AP prefix
// contains a space and -D defines split on it.
#define FL_FW_VERSION      "0.1.1"
#define FL_AP_PREFIX       "FireLabs S31"
#define FL_HOST_PREFIX     "fl"
#define FL_MQTT_TOPIC_ROOT "firelabs"
